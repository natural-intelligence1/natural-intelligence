# G.1.3b Pre-Verification Report

Date: 2026-05-06

## Check 1 — Identity model: `practitioners.id` IS `auth.users.id`

### Q1.1 — FK declarations on `practitioners`

```
constraint_name                  | column_name   | referenced_schema | referenced_table | referenced_column
---------------------------------+---------------+-------------------+------------------+------------------
practitioners_id_fkey            | id            | auth              | users            | id
practitioners_archived_by_fkey   | archived_by   | auth              | users            | id
practitioners_suspended_by_fkey  | suspended_by  | auth              | users            | id
practitioners_verified_by_fkey   | verified_by   | auth              | users            | id
```

**Result: PASS.** `practitioners.id` has a foreign key directly to `auth.users(id)`. The identity model is confirmed — `practitioners.id = auth.uid()` in all RLS policies is correct.

### Q1.2 — Migrated rows have valid auth user IDs

```
practitioner_id                       | auth_user_id                          | email                                         | status | display_name     | ids_match
--------------------------------------+---------------------------------------+-----------------------------------------------+--------+------------------+-----------
fb18b4bb-ef35-499c-a601-89775446243f  | fb18b4bb-ef35-499c-a601-89775446243f  | dr-lena-parrish-...@showcase.internal         | active | Dr Lena Parrish  | true
12719675-34e0-404d-8ca7-501b6d12a2c4  | 12719675-34e0-404d-8ca7-501b6d12a2c4  | dr-priya-nair-...@showcase.internal           | active | Dr Priya Nair    | true
e8ee62b0-1f94-4c52-8005-b52a6d2b6d12  | e8ee62b0-1f94-4c52-8005-b52a6d2b6d12  | dr-sarah-chen-...@showcase.internal           | active | Dr Sarah Chen    | true
4ae56f67-4c87-451d-974e-55a920b6b203  | 4ae56f67-4c87-451d-974e-55a920b6b203  | james-thornton-...@showcase.internal          | active | James Thornton   | true
5bb04a65-f5c6-4830-ac21-d068f98b2076  | 5bb04a65-f5c6-4830-ac21-d068f98b2076  | marcus-obi-...@showcase.internal              | active | Marcus Obi       | true
```

**Result: PASS.** 5 rows. All `ids_match = true`. No null `auth_user_id`.

### Q1.3 — Orphan check

```
orphan_count
------------
0
```

**Result: PASS.** Zero orphan practitioner rows.

---

## Check 2 — `practitioners_directory` view exposes safe columns only

### Q2.1 — View column list

| column_name              | data_type | Classification |
|--------------------------|-----------|----------------|
| id                       | uuid      | KEY            |
| display_name             | text      | ALLOWED        |
| bio                      | text      | ALLOWED        |
| credentials_summary      | text      | ALLOWED        |
| specialisations          | ARRAY     | ALLOWED        |
| modalities               | text      | ALLOWED        |
| years_experience         | integer   | ALLOWED        |
| experience_range         | text      | ALLOWED        |
| delivery_mode            | text      | ALLOWED        |
| city                     | text      | ALLOWED        |
| country                  | text      | ALLOWED        |
| primary_professions      | ARRAY     | ALLOWED        |
| area_tags                | ARRAY     | ALLOWED        |
| client_types             | ARRAY     | ALLOWED        |
| practice_name            | text      | ALLOWED        |
| tagline                  | text      | ALLOWED        |
| practitioner_tier        | text      | ALLOWED        |
| accepts_referrals        | boolean   | ALLOWED        |
| currently_seeing_clients | boolean   | ALLOWED        |
| profile_completeness_pct | integer   | ALLOWED        |
| is_directory_ready       | boolean   | ALLOWED        |

**Result: PASS.** All 21 columns are in the ALLOWED set. None of the FORBIDDEN columns (`website_url`, `linkedin_url`, `instagram_url`, `referral_contact_method`, `status`, `is_active`, `verified_by`, `suspended_by`, `suspension_reason`, `created_at`, `updated_at`, etc.) appear.

### Q2.2 — View definition

```sql
SELECT id,
    display_name,
    bio,
    credentials_summary,
    specialisations,
    modalities,
    years_experience,
    experience_range,
    delivery_mode,
    city,
    country,
    primary_professions,
    area_tags,
    client_types,
    practice_name,
    tagline,
    practitioner_tier,
    accepts_referrals,
    currently_seeing_clients,
    profile_completeness_pct,
    is_directory_ready
FROM practitioners
WHERE ((status = 'active'::text) AND (is_directory_ready = true));
```

**`security_invoker`:** Not set (`reloptions = null` → default `security_definer`). This is **correct by design**: the view runs as its owner (bypassing the practitioners table RLS for anon), and the privacy boundary is enforced by the view's own WHERE clause and column allowlist. Setting `security_invoker = true` would apply the practitioners table RLS to the anon caller — since no anon SELECT policy exists on `practitioners`, it would return zero rows and break the directory. The `security_definer` default is intentional.

### Q2.3 — Live anon-role queries

**Q2.3a — `SELECT id, display_name, delivery_mode FROM practitioners_directory LIMIT 3`:**
```
id                                    | display_name    | delivery_mode
--------------------------------------+-----------------+--------------
e8ee62b0-1f94-4c52-8005-b52a6d2b6d12 | Dr Sarah Chen   | online
5bb04a65-f5c6-4830-ac21-d068f98b2076 | Marcus Obi      | online
fb18b4bb-ef35-499c-a601-89775446243f | Dr Lena Parrish | both
```
**PASS** — returns rows.

**Q2.3b — `SELECT status FROM practitioners_directory LIMIT 1`:**
```
ERROR: column "status" does not exist
```
**PASS** — forbidden column not accessible through view.

**Q2.3c — `SELECT website_url FROM practitioners_directory LIMIT 1`:**
```
ERROR: column "website_url" does not exist
```
**PASS** — forbidden column not accessible.

**Q2.3d — `SELECT count(*) FROM practitioners` (as anon):**
```
direct_table_count
------------------
0
```
**PASS** — direct table access returns zero rows for anon (RLS blocks it).

---

## Verdict

All checks pass. Safe to proceed with G.1.3b helper module implementation.
